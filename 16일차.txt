ms -> 깃허브를인수함
아이티 공룡기업(ms, oracle, apple, google)이 어떤걸 인수했다.
이걸 빨리 공부해야함.


로켓펀치, 프로그래머스 즉.개발자 관련 구직사이트는
깃허브 연동을 만듬

앱 자바로 만들어짐
(구글스토어 -> 앱설치)
구글선포 ! 우리이제 더이상 자바지원안할거야(앱)


->자바에서는 객체를 class로 표현함
객체(class)는 고유한 특성(필드 변수)과 행동(메소드)을 가진다.
객체마다 특성이(필드변수) 행동(메소드) 이 다름
ex)기사  연애기사, 스포츠기사 등등나눔
****클래스는 필드변수와 메소드들로 구성

new라는 키워드를 이용해서 class를 불러옴(=객체화 했다.) -> 인스턴스 화 했다.
new를 써야 해당 클래스를 불러올수 있음.

자바는 클래스도 타입이다!

필드변수에 this를 붙혀 구분이가능!!!
(자바에만 있는게 아님)
(this: 자기 자신을 참조함)
ex)sql : self join

this -디폴트 퍼블릭

필드변수(전역변수)에는
접근지정자가 올 수 있다.
public
private
protected
default piblic

자바에는 하나의 퍼블릭 클래스만 올수 있음.
public class -> .java파일에 하번만 선언할수 있다.
public class는 파일 이름이다.
publc class 이름  {   
    ->이름과 { 사이를 상속 이라고한다.

디버깅
*********************************
1. 변수를 막 만든다.
2.디버깅을 한다.
-> 변수가없는 데이터는 디버깅 떄 무시함.
3.불필요한,중복되는  변수를 제거한다
**********************************